<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.master.CityMapper">
	<select id="listCitys" resultType="java.util.Map">
		select * from city
	</select>

	<insert id="insertUser">
		INSERT INTO USER(userName,account)VALUE("1","3");
	</insert>

</mapper>